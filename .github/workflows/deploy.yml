name: Deploy Frontend to Firebase Hosting

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy.yml'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: 'frontend/package-lock.json'

      - name: Install Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Ensure PMTiles Data Assets
        working-directory: ./frontend
        run: |
          mkdir -p public/data
          if [ ! -f "public/data/india_matched_hexbins.pmtiles" ]; then
            echo "Fetching PMTiles archive from mirror..."
            curl -fL "https://thermalwatch-india-3591.web.app/data/india_matched_hexbins.pmtiles" -o "public/data/india_matched_hexbins.pmtiles"
          fi

      - name: Build Production Bundle
        working-directory: ./frontend
        run: npm run build

      - name: Deploy to Firebase Hosting (sih26ekaant)
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_THERMALWATCH_INDIA_3591 }}'
          channelId: live
          target: sih26ekaant
          entryPoint: ./frontend
